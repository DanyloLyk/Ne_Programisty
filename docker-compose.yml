version: '3.8'

services:
  web:
    # Збираємо контейнер з поточної папки
    build: .
    container_name: shop_container
    # Прокидаємо порт: "мій комп'ютер 5000" -> "контейнер 5000"
    ports:
      - "5000:5000"
    # Налаштування (з файлу .env)
    env_file:
      - .env
    # "Флешка" для бази даних. Якщо видалимо контейнер, дані залишаться тут.
    volumes:
      - sqlite_data:/app/data
    # Якщо впаде сервер - перезапусти
    restart: unless-stopped

# Оголошення "флешки" (тому що ми просили Volume)
volumes:
  sqlite_data: