# 📦 Лабораторна робота №8: Контейнеризація проєкту

## 📂 Інформація про проєкт

### **🚀 Команда:** `Ne_Programisty`
**Тема:** Інтернет-магазин настільних ігор **"Гральна Комора"** 🎲

### **👨‍💻 Команда розробників (Dream Team):**
* 🦁 **Лукащук Данило** (Team Lead)
* 🐼 **Кондратюк Дмитро**
* 🦊 **Чорноус Сергій**
* 🐻 **Базюк Максим**

## 📝 Огляд проєкту
У рамках цієї лабораторної роботи ми виконали повну **контейнеризацію** нашого веб-застосунку на Flask. Метою було створення ізольованого, відтворюваного середовища, яке легко розгортається на будь-якій машині без "пекла залежностей". 

Ми використали **Docker** для пакування застосунку та **Docker Compose** для оркестрації сервісів (Flask Backend + Nginx Web Server).



## 🏗 Архітектура контейнерного рішення

### 🐳 Docker образ
Ми створили оптимізований образ для нашого Python-бекенду.

* **Базовий образ:** `python:3.11-alpine` (обрано версію Alpine Linux для мінімізації розміру).
* **Розмір фінального образу:** ~120 MB (проти 900 MB+ у стандартних образах).
* **Використання багатоетапної збірки:** ✅ **Так** (Multi-stage build).

### 🐙 Docker Compose
Наша інфраструктура складається з двох взаємопов'язаних сервісів:

* **Кількість сервісів:** 2
    1.  `web` (Flask Application) — внутрішній контейнер з бізнес-логікою (порт 5000).
    2.  `nginx` (Reverse Proxy) — вхідна точка для клієнта (порт 80).
* **Використовувані volumes:**
    1.  `sqlite_data` — для постійного збереження файлу бази даних (`database.db`).
    2.  `static_volume` — спільний том для роздачі статичних файлів (CSS, JS, Images).

## 💡 Прийняті рішення та обґрунтування

### 🐧 Вибір базового образу (`python:3.11-alpine`)
Ми відмовилися від стандартного `python:3.11` (Debian-based) на користь **Alpine Linux**.
* **Причина:** Alpine важить всього ~5 МБ. Це дозволяє значно зменшити час завантаження образу та зекономити місце на диску. Також менша кількість встановлених системних утиліт зменшує поверхню атаки (Security).

### 💾 Організація збереження даних (Volumes)
Оскільки SQLite зберігає дані у файлі, при видаленні контейнера всі дані користувачів зникли б.
* **Рішення:** Ми використали **Named Volume** (`sqlite_data`), який монтується в папку `/app/data` всередині контейнера.
* **Результат:** Навіть якщо виконати `docker-compose down`, файл бази даних залишається у безпеці на хост-машині і підтягується при наступному запуску.

### ⚡ Оптимізації
1.  **Multi-stage build:**
    * *Stage 1 (Builder):* Встановлюємо важкі компілятори (`gcc`, `musl-dev`), збираємо бібліотеки.
    * *Stage 2 (Final):* Копіюємо тільки готові бібліотеки. Сміття від компіляції не потрапляє у фінал.
2.  **Reverse Proxy (Nginx):**
    * Flask (`gunicorn`/`werkzeug`) не призначений для прямого доступу з інтернету. Ми сховали його за **Nginx**, який працює на порту 80, забезпечує безпеку та маршрутизацію.
3.  **Healthchecks:**
    * Налаштували перевірку здоров'я контейнера (`wget ... || exit 1`), щоб Docker автоматично знав, чи живий наш застосунок.
4.  **.dockerignore:**
    * Виключили `venv`, `.git`, `__pycache__`, щоб не копіювати зайві файли в контейнер і пришвидшити збірку context.


## 🚀 Інструкції з розгортання

Для запуску проєкту на чистій системі (потрібен лише встановлений Docker):

1.  **Клонуйте репозиторій:**
    ```bash
    git clone <your-repo-link>
    cd Ne_Programisty
    ```

2.  **Створіть файл змінних оточення `.env`:**
    ```env
    FLASK_APP=app.py
    DATABASE_PATH=/app/data/database.db
    SECRET_KEY=super_secret_key_123
    JWT_SECRET_KEY=super_jwt_key_456
    ```

3.  **Запустіть проєкт однією командою:**
    ```bash
    docker-compose up -d --build
    ```

4.  **Перевірте роботу:**
    Відкрийте браузер за адресою: **http://127.0.0.1** (або `localhost`).

5.  **Зупинка проєкту:**
    ```bash
    docker-compose down
    ```

## 📈 Можливі покращення
Що можна додати в майбутньому для Production-рівня:
* 🔒 **HTTPS:** Додати Certbot (Let's Encrypt) у Nginx для захищеного з'єднання.
* 🐘 **PostgreSQL:** Замінити SQLite на повноцінну серверну БД (окремим контейнером) для кращої продуктивності при багатьох користувачах.
* 📦 **Redis:** Додати контейнер з Redis для кешування запитів.

## 🏁 Висновки
У ході лабораторної роботи ми успішно контейнеризували RESTful API магазин.
Ми навчилися:
* Писати оптимізовані `Dockerfile` (multi-stage).
* Використовувати `docker-compose` для зв'язку кількох сервісів (Flask + Nginx).
* Керувати даними через Volumes.
* Вирішувати проблеми з правами доступу та мережевою взаємодією всередині Docker.

Тепер наш проєкт є **Cloud-ready** і може бути розгорнутий на будь-якому сервері за лічені хвилини. 🐳🚀